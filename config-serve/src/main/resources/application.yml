server:
  port: 8085
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Guino-Elvis/PROYECTO_MOBILE_C8_PERSONALISADO.git
          search-paths: config-data
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t
  
